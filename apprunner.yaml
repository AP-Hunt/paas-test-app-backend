---
version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install

run:
  command: python -m demo_backend
  env:
    - name: DATABASE_URL
      value: "postgresql://localhost:5432"
    - name: DEBUG
      value: 1
